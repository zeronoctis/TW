{#
{{form(form, {'attr':{'novalidate': 'novalidate'}} )}}
#}

{{ form_start(form) }}
	{{ form_errors(form) }}

	{{ form_row(form.task) }}
	{{ form_row(form.dueDate) }}
{{ form_end(form) }}

{{form.vars.value.task}}